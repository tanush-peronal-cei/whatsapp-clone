name: Run PowerShell Script

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Run PowerShell script
      run: pwsh ./scripts/powershell-script.ps1
